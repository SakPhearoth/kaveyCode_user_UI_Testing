on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: nextjs-kavey-user

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: DOCKER_REPOSITORY
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: USE with SHA
        run: | 
          echo "TAG=${GITHUB_SHA::8}" >> $GITHUB_ENV
      - name: Show the TAGS values
        run: |
          echo "TAG value is : ${{env.TAG}}"
      - name: Login to Docker Registry
        run: |
          echo "${{ secrets.PASSWORD }}" | docker login -u "${{ secrets.USERNAME }}" --password-stdin
      - name: Builder Docker Image
        run: docker build -t ${{ secrets.USERNAME }}/${{ env.IMAGE_NAME }}:v1 .
      - name: Push the Docker Image
        run: docker push ${{ secrets.USERNAME }}/${{ env.IMAGE_NAME }}:v1
      - name: Logout from the Registry
        if: always()
        run: |
          docker logout
